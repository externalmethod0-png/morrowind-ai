# Двоичные файлы llama.cpp сюда не кладём — это 667 МБ чужой сборки.
# Качается со страницы релизов ggml-org/llama.cpp: сборка под win-cuda,
# версия CUDA должна совпадать с драйвером (nvidia-smi покажет).
*
!.gitignore
!*.bat
